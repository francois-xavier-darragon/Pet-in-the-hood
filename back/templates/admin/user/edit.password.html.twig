{% set title = 'Modifier mon profil' %}
{% set userId = app.request.attributes.get('id') %}
{% extends 'base.html.twig' %}

{% block title %}{{ title }} - {{ parent() }}{% endblock %}

{% block body %}

    <div class="col-xl-10 col-12 mx-auto">
        <div class="container-xxl bg-dark mt-3 p-3 rounded text-white">
            <div class="row">
                <h2 class="border-3 border-bottom border-success pb-2"><span class="fw-bold text-success">&sol;&sol;</span> {{ title }}</h2>
            </div>
            <div class="row justify-content-center mt-3">
                <div class="col-8">
                    {{ form_start(form) }}
                    {{ form_row(form.password.first, {'label': 'Mot de passe'}) }}
                    {{ form_row(form.password.second, {'label': 'Répetez le mot de passe'}) }}
                    <button class="btn btn-success">Modifier</button> <a class="btn btn-primary" href="{{ path('admin_user_edit', {id: userId}) }}">Retour</a>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
